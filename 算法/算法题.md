## 遍历叶子节点

```c++
    bool hasPathSum_(TreeNode* root, int sum, int targetSum) {
        if (root == nullptr) {	// 输入为null情况
        }
        // 叶子节点
        if (root->left == nullptr && root->right == nullptr) {	
        }
        
        // 非叶子节点
        if (root->left) {	// 为null就不遍历了
        }
        if (root->right) {
        }
    }
```

